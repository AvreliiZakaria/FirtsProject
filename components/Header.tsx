"use client";
import Link from "next/link";
import { Coins, LogIn, User } from "lucide-react";
import { useBalance } from "@/components/Providers";
export function Header(){const {balance,loggedIn,loading}=useBalance();const right=loggedIn?<div className="flex items-center gap-2"><Link href="/profile" className="inline-flex min-h-9 items-center gap-2 rounded-full border border-base-line bg-base-raised px-3 py-1.5 text-xs font-semibold text-ink"><Coins size={14} className="text-amber-200"/><span>{balance??0} монет</span></Link><Link href="/profile" aria-label="Мой кабинет" className="flex h-9 w-9 items-center justify-center rounded-full border border-base-line bg-base-raised text-ink"><User size={15}/></Link></div>:<Link href="/login" className="inline-flex min-h-9 items-center gap-2 rounded-full bg-ink px-3.5 py-1.5 text-xs font-semibold text-base"><LogIn size={14}/><span>Войти</span></Link>;return <header className="sticky top-0 z-30 border-b border-base-line/70 bg-base/90 px-4 py-3 backdrop-blur-xl md:px-8 lg:px-12"><div className="mx-auto flex w-full max-w-content items-center justify-between"><Link href="/" aria-label="Lumo, на главную" className="group flex items-center gap-2.5"><span className="flex h-8 w-8 items-center justify-center rounded-[10px] bg-ink text-base"><span className="text-[19px] font-black leading-none">L</span></span><div className="leading-none"><p className="text-[15px] font-bold text-ink">Lumo</p><p className="mt-1 text-[8px] font-medium uppercase tracking-[0.18em] text-ink-faint">AI portraits</p></div></Link>{loading?<span className="h-9 w-24 animate-pulse rounded-full bg-base-raised"/>:right}</div></header>}
